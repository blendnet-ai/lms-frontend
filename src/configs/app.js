const appConfig = {
  MAX_WRITING_WORDS: 250,
  MIN_SPEAKING_SECONDS: 30,
  MAX_SPEAKING_SECONDS: 60,
  RETRY_ATTEMPTS: 3, // will ask the user to retry till this is reached, otherwise ask to check their internet
};

export default appConfig;
